// @ts-nocheck
import type {u53} from '~/common/types';

/**
 * An enum whose members are numbered implicitly, requesting conversion helpers.
 *
 * @generate convert
 */
export const enum MessageDirection {
    INBOUND,
    OUTBOUND,
}
