// @ts-nocheck
import type {u53} from types;

/**
 * A numeric enum in a schema whose import specifier is not a string literal, which is rejected.
 */
export const enum MessageDirection {
    INBOUND = 0,
}
