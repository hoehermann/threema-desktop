{
  "$schema": "https://www.schemastore.org/tsconfig",
  "extends": "@threema/ts-config/tsconfig.node22.json",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true
  },
  // Note: The schema fixtures are input data, not sources, and are deliberately not type checked.
  "include": ["src/**/*.mjs", "test/**/*.mjs", "vitest.config.mjs"]
}
